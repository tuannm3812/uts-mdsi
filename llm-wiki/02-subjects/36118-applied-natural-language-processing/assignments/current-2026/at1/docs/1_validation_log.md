# AT1 Extraction and Theme Validation Log

## 1. Validation Protocol

The audit was run on 28 July 2026 after the first complete baseline.

- **Extraction check:** inspect every document with fewer than 500 characters
  from native PDF extraction.
- **Match check:** use NumPy seed `42` to select two matching submissions per
  theme, then read the match in its surrounding source context.
- **Recall probe:** search documents without a dictionary match for nearby
  concepts that could reveal a false negative.
- **Decision rule:** a true match must discuss the analytical theme, rather
  than merely contain a word in an affiliation, citation, inquiry heading, or
  unrelated context.

This is a targeted validation audit, not a formal estimate of classifier
accuracy. There is no fully annotated gold-standard corpus.

## 2. Image-Based PDFs

Native extraction showed that four PDFs were not genuinely short; their page
content was stored as images.

| Submission | Pages | Native characters | OCR characters | Decision |
|---|---:|---:|---:|---|
| `Sub098` | 6 | 425 | 14,171 | OCR recovered RDA Gippsland submission; retain for manual review |
| `Sub110` | 4 | 287 | 9,379 | OCR recovered Australian Chamber of Commerce and Industry submission |
| `Sub134` | 3 | 215 | 4,955 | OCR recovered Northern Territory ministerial submission |
| `Sub141` | 1 | 73 | 2,510 | OCR recovered Hazara community proposal, but spacing is degraded |

The ignored files in `data/ocr_text/` are generated by
`scripts/ocr_low_text_pdfs.py`. They support inspection, but the four
submissions remain outside quantitative prevalence and per-word calculations
because OCR token boundaries are not directly comparable with native text.

## 3. Match Audit

| Submission | Theme | Context checked | Judgment |
|---|---|---|---|
| `Sub014` | Economic contribution | Seafood-industry economic contribution and productivity | True match |
| `Sub109` | Economic contribution | Productivity and innovation in the knowledge economy | True match |
| `Sub063` | Skills and services | Employer workforce shortages | True match |
| `Sub062` | Skills and services | Skills gaps and critical-sector shortages | True match |
| `Sub094` | Regional development | Structural skills crisis in regional Australia | True match |
| `Sub016` | Regional development | Regional job vacancies and occupations | True match |
| `Sub010` | Worker protection | Workers described as vulnerable to exploitation | True match |
| `Sub077` | Worker protection | Regional partnerships explaining worker protections | True match |
| `Sub100` | Permanent pathways | Employer nomination and permanent residency | True match |
| `Sub106` | Permanent pathways | Regional retention outcomes | True match |
| `Sub068` | Recognition and processing | Visa processing times and permanence | True match |
| `Sub111` | Recognition and processing | Published processing times for skilled visas | True match |
| `Sub118` | Housing and infrastructure | Explicit housing and infrastructure constraints | True match |
| `Sub065` | Housing and infrastructure | Funding housing and infrastructure solutions | True match |

All 14 sampled positive matches were substantively relevant in context. This
supports precision for the revised patterns, but the small sample does not
prove that every corpus match is correct.

## 4. Non-Match and Recall Probes

| Submission | Probe | Judgment | Action |
|---|---|---|---|
| `Sub003` | “employment opportunities” without an economic-theme match | Correct non-match; field-placement context, not an economic-value claim | None |
| `Sub008` | “strategic vulnerability” without a worker-protection match | Correct non-match; national capability context, not worker vulnerability | None |
| `Sub072` | “long-term settlement” without a permanence match | False negative; clearly expresses durable settlement | Add bounded `long-term settlement` pattern |
| `Sub022` | Qualifications and registration without a recognition match | Ambiguous; describes New Zealand eligibility rather than an Australian recognition process | Keep unmatched and note boundary |

## 5. Changes Made After Validation

1. Removed bare `education`, `health`, `investment`, and `infrastructure`
   patterns because they produced affiliation, citation, or terms-of-reference
   matches.
2. Replaced them with policy phrases such as `education workforce`,
   `economic contribution`, and `infrastructure capacity`.
3. Replaced the unbounded `pathway.*permanen` expression with a bounded phrase
   pattern so a match cannot span large portions of a normalised document.
4. Added worker-protection phrases including `underpay`, `workplace rights`,
   and `worker protections`.
5. Added `long-term settlement` following the `Sub072` false-negative review.
6. Kept the quantitative denominator at 139 native-text documents and made the
   OCR exclusion explicit.

After refinement, the leading themes remain **skills and essential services**
(approximately 92.8% of usable documents) and **economic contribution**
(approximately 85.6%). The changes therefore improve measurement precision
without reversing the main finding.

## 6. Submitter Metadata and Group Comparison

The curated file `validation/submitter_metadata.csv` contains one row for each
of the 139 observed main submission numbers. The four supplements inherit their
parent's metadata through a many-to-one join. The notebook asserts that the
metadata identifiers exactly equal the observed main identifiers before any
group analysis runs.

Names were transcribed from the supplied PDFs where visible. Visual headers
were used for seven image-based or logo-led documents. Four unsigned documents
are labelled `Name not publicly stated`; `Sub079` remains `Unclassified`
because the supplied PDF begins mid-submission. `Sub140.pdf` is retained under
its supplied identifier and flagged because its internal footers say
`Submission 121`.

The submitter taxonomy records the primary represented role:

1. Industry and employer
2. Government and public body
3. Education and research
4. Community and advocacy
5. Professional association
6. Individual
7. Migration and legal services
8. Union and worker representative
9. Unclassified

`classification_basis` records the reason for each assignment. The comparison
uses one native-text main submission per row, excludes supplements, and omits
the unresolved category. Percentages are document prevalence within each
group. They describe this self-selected archive only; they are not population
estimates, measures of stance, or causal effects.

## 7. Balanced Group-Contrast Check

The largest or most interpretable percentage-point differences were checked
against passages from both sides of each comparison and recorded in
`validation/group_contrast_excerpts.csv`.

- The union worker-protection result is supported by `Sub112`, which explicitly
  links equal workplace rights and protection from exploitation.
- The education comparison in `Sub109` emphasises science, innovation, and
  workforce capability. This illustrates a different emphasis but does not
  establish theme absence across a document or group.
- `Sub014` connects an industry workforce claim to rural and regional
  communities, supporting the regional-development contrast.
- `Sub042` explicitly calls for recognition reform, while `Sub135` confirms
  that the lower individual-group recognition rate still contains important
  within-group exceptions.

The notebook labels these as qualitative checks rather than statistical tests.
Each reported comparison retains the usable group denominator, and no excerpt
is treated as representative of every member of its category.

## 8. Sentence-Level Stance-Cue Audit

The stance extension is multi-label and sentence-level. A theme-bearing
sentence may contain **Benefit**, **Concern**, and **Recommendation / condition**
cues simultaneously. Sentences without a configured cue remain
`Context only / unclear`; they are not forced into a position.

Using NumPy seed `42`, five initial examples from each detected label were
manually reviewed in `validation/stance_validation_audit.csv`. Eighteen of the
20 sampled labels were supported:

1. `Sub006` copied the inquiry purpose, so a benefit word in that boilerplate
   did not represent the submitter's stance. The known purpose wording is now
   excluded.
2. `Sub033` called skilled migration a “practical and timely solution,” which
   the initial benefit patterns missed. A bounded `solution` cue was added.

The audit also preserves multi-label human judgments even when only one
sampled label is being evaluated. It is a development check, not an unbiased
accuracy estimate. The output describes rhetorical cues rather than whole
documents, cannot reliably resolve negation or third-party quotations, and
does not justify labelling a submitter as simply supportive or opposed.
